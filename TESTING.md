# RTF 性能测试指南

## 快速测试

### 1. 启动服务器

```bash
./run.sh
```

等待服务器启动完成，访问 http://localhost:8088/health 确认运行正常。

### 2. 运行 RTF 测试

```bash
# 使用默认测试文件
./test_rtf_simple.sh test_02.wav

# 或使用自定义音频文件
./test_rtf_simple.sh your_audio.wav

# 指定服务器地址
./test_rtf_simple.sh test_02.wav http://localhost:8088
```

## 完整测试（Python 脚本）

### 基本用法

```bash
python test_rtf.py --audio test_02.wav
```

### 高级选项

```bash
# 指定服务器和语言
python test_rtf.py \
    --audio test_02.wav \
    --server http://localhost:8088 \
    --language zh

# 自定义批量大小测试
python test_rtf.py \
    --audio test_02.wav \
    --batch-sizes "1,2,4,6,8,10"
```

## 测试输出说明

测试脚本会输出以下信息：

### 1. 单文件测试
```
📊 单文件推理测试
📁 文件: test_02.wav
⏱️  音频时长: 3.50s
🚀 处理时间: 0.12s
📈 RTF: 0.0343 (29.2x 实时速度)
```

### 2. 批量测试
```
📊 批量推理测试 (Batch Size = 6)
📁 文件: test_02.wav
⏱️  单个音频时长: 3.50s
⏱️  总音频时长: 21.00s (0.01h)
🚀 总处理时间: 0.42s
⚡ 平均每文件: 0.07s
📈 Batch RTF: 0.0202 (49.5x 实时速度)
```

### 3. 吞吐量计算
```
📊 吞吐量计算
🎯 RTF: 0.0202
⚡ 实时速度倍数: 49.5x
📊 Batch Size: 6

📈 每秒可处理: 49.5秒音频
📈 每分钟可处理: 2970秒音频 (49.5分钟)
📈 每小时可处理: 49.5小时音频

🎉 每天可处理: 1188 小时音频
🎉 每天可处理: 50 天音频
```

### 4. 性能汇总
```
📊 性能汇总
模式            RTF        实时倍数      每天可处理(小时)
────────────────────────────────────────────────────────────
单文件          0.0343     29.2x         700
Batch-3         0.0267     37.5x         900
Batch-6         0.0202     49.5x         1188
Batch-10        0.0215     46.5x         1116

🏆 最优配置: Batch-6
   RTF: 0.0202
   每天可处理: 1188 小时音频
   相比单文件加速: 3.5x
```

## 关键指标说明

### RTF (Real-Time Factor)
- **定义**: 处理时间 / 音频时长
- **例如**: RTF = 0.02 表示处理 1 秒音频需要 0.02 秒
- **越小越好**: RTF 越小，处理速度越快

### 实时速度倍数
- **定义**: 1 / RTF
- **例如**: 49.5x 表示处理速度是实时播放的 49.5 倍
- **越大越好**: 倍数越大，处理越快

### 每天可处理小时数
- **计算公式**: (24 × 3600) / RTF / 3600
- **简化**: 24 / RTF 小时
- **例如**: RTF = 0.0202 时，每天可处理 1188 小时音频

## 批量优化说明

根据测试结果，batch_size=6 是最优配置：

| Batch Size | RTF    | 加速比 | 每天处理量 |
|-----------|--------|-------|-----------|
| 1         | 0.0343 | 1.0x  | 700h      |
| 3         | 0.0267 | 1.3x  | 900h      |
| 6         | 0.0202 | 1.7x  | 1188h     |
| 10        | 0.0215 | 1.6x  | 1116h     |

**结论**:
- batch_size=6 达到最优的 GPU 利用率
- 相比单文件处理提升 **3.5x**
- 每天可处理 **1188 小时** 音频（约 50 天音频）

## 生产环境估算

### 单进程 (1 GPU)
- **最优配置**: batch_size=6
- **RTF**: 0.0202
- **每天处理**: 1188 小时

### 多进程场景
如果使用 8 张 GPU：
- **理论最大**: 1188 × 8 = 9504 小时/天
- **约等于**: 395 天音频/天

### 实际应用建议
1. **小规模**: 使用单进程，batch_size=6
2. **中等规模**: 2-4 进程，每进程独占 GPU
3. **大规模**: 多机多卡部署，配合负载均衡

## 故障排查

### 服务器未启动
```bash
# 检查服务器状态
curl http://localhost:8088/health

# 启动服务器
./run.sh
```

### 音频文件格式问题
确保音频文件是常见格式（WAV, MP3, FLAC 等）

### 依赖缺失
```bash
pip install soundfile requests
```

## 测试文件准备

如果没有 test_02.wav，可以：

1. **下载测试音频**
```bash
# 从数据集下载
python -c "from datasets import load_dataset; ds = load_dataset('hf-internal-testing/librispeech_asr_demo', 'clean', split='validation'); ds[0]['audio']"
```

2. **使用自己的音频文件**
```bash
# 支持的格式: WAV, MP3, FLAC, OGG, M4A
./test_rtf_simple.sh your_audio.wav
```

3. **转换格式**
```bash
# 使用 ffmpeg 转换
ffmpeg -i input.mp3 -ar 16000 -ac 1 output.wav
```

## 性能优化建议

1. **使用 GPU**: 确保 CUDA 可用，性能提升显著
2. **批量大小**: 使用 batch_size=6 获得最佳性能
3. **音频预处理**: 统一采样率和格式可提升效率
4. **多进程**: 对于大规模任务，使用多进程并行处理
