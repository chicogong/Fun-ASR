# Fun-ASR 进一步优化路线图

当前性能: **807 小时/天** (批量45, RTF 0.0297)
GPU 利用率: **7%** (3.1 / 44.5 GB)

理论极限: **11,600 小时/天** (14.4x 提升)

---

## 📊 优化方案对比

| 方案 | 难度 | 时间 | 提升 | 日处理 | 累计提升 | 状态 |
|------|------|------|------|--------|----------|------|
| **当前** | - | - | - | 807h | - | ✅ |
| 1. 多Worker | ⭐ | 30min | 2-3x | 1,600-2,400h | 2-3x | 🔨 已实现 |
| 2. 并发优化 | ⭐⭐ | 测试 | 1.5x | 1,200h | 1.5x | ⚡ 可用 |
| 3. FP16精度 | ⭐⭐⭐ | 1h | 1.5-2x | 2,400-4,800h | 3-6x | 📝 |
| 4. 批量并行 | ⭐⭐⭐⭐ | 3h | 2-3x | 4,800-14,400h | 6-18x | 📝 |
| 5. 异步队列 | ⭐⭐⭐⭐⭐ | 2d | 3-5x | 14,400-72,000h | 18-90x | 📝 |

---

## 🚀 方案 1: 多 Worker 模式（已实现）

### 原理
运行多个 uvicorn worker 进程，每个 worker 独立处理请求

### 实施
```bash
# 使用优化版本服务器
python server_optimized.py

# 或设置环境变量
NUM_WORKERS=4 python server_optimized.py
```

### 预期效果
- **2-3x 性能提升**
- 日处理: 807h → **1,600-2,400h**
- GPU 利用率: 7% → **15-20%**

### 配置建议
| GPU | 推荐 Workers | 预期日处理 |
|-----|-------------|------------|
| L20 (44.5GB) | 4 | 2,400h |
| L20 (44.5GB) | 8 | 3,600h |
| T4 (15GB) | 2 | 1,200h |
| T4 (15GB) | 4 | 1,800h |

---

## ⚡ 方案 2: 并发请求（已测试可用）

### 原理
客户端并发发送多个请求，服务器并行处理

### 测试结果（之前的测试）
- 并发 16: RTF 0.0291, 日处理 **826h**
- 吞吐量: 3.44 请求/秒

### 组合效果
多 Worker (4个) + 并发 (16) = **3,200h+/天**

---

## 🎮 方案 3: FP16 半精度推理

### 原理
使用 FP16 (半精度) 替代 FP32 (单精度)

### 实施
```python
# 在模型加载后添加
if device == "cuda:0":
    model = model.half()  # 转换为 FP16
```

### 预期效果
- **1.5-2x 性能提升**
- GPU 内存使用减少 **50%**
- 更多并发空间

### 风险
- 精度可能略有下降（通常可忽略）
- 需要测试验证准确率

---

## 🔄 方案 4: 真正的批量并行

### 当前问题
```python
# 当前是顺序处理
for i, tmp_path in enumerate(tmp_paths):
    result = model.generate(input=[tmp_path], ...)  # 一个一个处理
```

### 优化方案
```python
# 真正的批量推理
results = model.generate(
    input=tmp_paths,  # 一次性处理所有
    batch_size=len(tmp_paths),
    ...
)
```

### 挑战
- MLT 模型可能不支持真正的批量
- 需要测试和验证

### 预期效果
- **2-3x 性能提升**（如果模型支持）
- GPU 利用率提升到 **30-50%**

---

## 🌊 方案 5: 异步队列 + 动态批处理

### 架构
```
客户端请求 → 异步队列 → 批处理聚合器 → 并行推理 → 返回结果
                ↓
        每 50ms 或 N 个请求触发
```

### 特性
- 请求自动聚合成批
- 动态调整批量大小
- 最小化延迟，最大化吞吐

### 预期效果
- **5-10x 性能提升**
- GPU 利用率: **50-80%**
- 日处理: **4,000-8,000h**

### 实施复杂度
- 需要重构服务器架构
- 实现异步队列系统
- 复杂的批处理调度逻辑

---

## 📋 推荐实施顺序

### 阶段 1: 快速提升（今天完成）
1. ✅ 部署 `server_optimized.py` (多 Worker)
2. ✅ 测试性能
3. ✅ 验证稳定性

**预期**: 807h → **1,600-2,400h** (2-3x)

### 阶段 2: 中期优化（1-2天）
1. 实施 FP16 精度
2. 测试精度损失
3. 调优批量大小

**预期**: 2,400h → **3,600-4,800h** (4-6x)

### 阶段 3: 高级优化（3-5天）
1. 研究真正的批量并行
2. 实施异步队列
3. 性能调优

**预期**: 4,800h → **8,000-12,000h** (10-15x)

---

## 🎯 立即行动方案

### 部署多 Worker 版本

#### Docker 部署
```bash
# 更新 Dockerfile 使用 server_optimized.py
# 然后部署
docker stop funasr && docker rm funasr

docker run -d --name funasr \
  --gpus all \
  -p 8000:8000 \
  -e USE_GPU=true \
  -e MAX_BATCH_SIZE=50 \
  -e NUM_WORKERS=4 \
  -v ~/.cache/modelscope:/root/.cache/modelscope \
  ccr.ccs.tencentyun.com/chico/funasr-mlt-batch:latest
```

#### 本地部署
```bash
cd /data/workspace/Fun-ASR
NUM_WORKERS=4 MAX_BATCH_SIZE=50 USE_GPU=true python server_optimized.py
```

### 测试验证
```bash
# 1. 检查 workers
curl http://localhost:8000/health

# 2. 性能测试
cd tests
python test_remote_optimized.py

# 3. 并发测试
python optimize_l20.py
```

---

## 📊 性能预测

| 配置 | RTF | 日处理 | GPU% | 实现难度 |
|------|-----|--------|------|----------|
| 当前 | 0.0297 | 807h | 7% | ✅ |
| +多Worker(4) | 0.0074 | 2,400h | 20% | ⭐ |
| +FP16 | 0.0037 | 4,800h | 30% | ⭐⭐⭐ |
| +批量并行 | 0.0012 | 14,400h | 60% | ⭐⭐⭐⭐ |
| +异步队列 | 0.0007 | 24,000h | 80% | ⭐⭐⭐⭐⭐ |

---

## 💡 性能对比

### 月处理能力
| 配置 | 小时/月 | 天数等价 |
|------|---------|----------|
| 当前 | 24,210h | 1,008天 |
| +Worker | 72,000h | 3,000天 |
| +FP16 | 144,000h | 6,000天 |
| 极限优化 | 720,000h | 30,000天 |

### 年处理能力
当前: **294,555 小时** (12,273 天)
优化后: **2,160,000-8,760,000 小时** (90,000-365,000 天)

---

## ⚠️ 注意事项

1. **GPU 内存**
   - 每个 Worker 加载一次模型
   - 4 Workers ≈ 8-12 GB GPU 内存
   - L20 (44.5GB) 可以运行 10+ Workers

2. **CPU 核心**
   - Workers 需要 CPU 资源
   - 建议: Workers ≤ CPU 核心数

3. **稳定性**
   - 逐步增加 Workers
   - 监控 GPU 内存和温度
   - 压力测试验证

4. **成本**
   - 多 Worker 增加内存开销
   - FP16 减少内存使用
   - 需要权衡

---

## 🎯 目标设定

### 短期目标（本周）
- ✅ 实现多 Worker
- ✅ 日处理突破 2,000h
- ✅ GPU 利用率提升到 20%

### 中期目标（本月）
- 实现 FP16 优化
- 日处理突破 5,000h
- GPU 利用率提升到 50%

### 长期目标（季度）
- 完整异步队列系统
- 日处理突破 10,000h
- GPU 利用率达到 80%

---

**当前状态**: 807h/天, 7% GPU
**理论极限**: 11,600h/天, 80% GPU
**提升空间**: **14.4 倍**

立即部署多 Worker 版本，即可获得 **2-3 倍**性能提升！
